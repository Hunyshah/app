// Centralized theme tokens for ChatGPT-style UI
export const colors = {
  primaryBg: "#343541", // main background
  sidebarBg: "#202123", // sidebar background
  accent: "#10A37F", // highlight accents (send button, links)
  textPrimary: "#FFFFFF", // primary text
  textSecondary: "#ABB2BF", // secondary text
  inputBg: "#444654", // input and card background
  border: "#5A5F70", // borders/dividers
};

export const typography = {
  fontFamily: "Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, \"Apple Color Emoji\", \"Segoe UI Emoji\"",
  sizes: {
    xs: "0.75rem",
    sm: "0.875rem",
    base: "1rem",
    lg: "1.125rem",
    xl: "1.25rem",
  },
  weights: {
    regular: 400,
    medium: 500,
    semibold: 600,
    bold: 700,
  },
};

export const layout = {
  sidebarWidth: 280,
  maxContentWidth: 1200,
};